import 'package:flutter/material.dart';

class CustomRoomBookingModalSheet extends StatelessWidget {
  const CustomRoomBookingModalSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}